/*********************************************************************
*                                                                    *
*                SEGGER Microcontroller GmbH & Co. KG                *
*        Solutions for real time microcontroller applications        *
*                                                                    *
**********************************************************************
*                                                                    *
* C-file generated by:                                               *
*                                                                    *
*        GUI_Builder for emWin version 5.44                          *
*        Compiled Nov 10 2017, 08:53:57                              *
*        (c) 2017 Segger Microcontroller GmbH & Co. KG               *
*                                                                    *
**********************************************************************
*                                                                    *
*        Internet: www.segger.com  Support: support@segger.com       *
*                                                                    *
**********************************************************************
*/

// USER START (Optionally insert additional includes)
#include <stdio.h>
#include <stdlib.h>
// USER END

#include "DIALOG.h"

/*********************************************************************
*
*       Defines
*
**********************************************************************
*/
#define ID_WINDOW_0    (GUI_ID_USER + 0x00)
#define ID_EDIT_0    (GUI_ID_USER + 0x01)
#define ID_EDIT_1    (GUI_ID_USER + 0x02)
#define ID_TEXT_0    (GUI_ID_USER + 0x03)
#define ID_BUTTON_0    (GUI_ID_USER + 0x04)
#define ID_BUTTON_1    (GUI_ID_USER + 0x05)
#define ID_BUTTON_2    (GUI_ID_USER + 0x06)
#define ID_BUTTON_3    (GUI_ID_USER + 0x07)
#define ID_BUTTON_4    (GUI_ID_USER + 0x08)
#define ID_BUTTON_5    (GUI_ID_USER + 0x09)
#define ID_BUTTON_6    (GUI_ID_USER + 0x0A)
#define ID_BUTTON_7    (GUI_ID_USER + 0x0B)
#define ID_BUTTON_8    (GUI_ID_USER + 0x0C)
#define ID_BUTTON_9    (GUI_ID_USER + 0x0D)
#define ID_BUTTON_10    (GUI_ID_USER + 0x0E)
#define ID_BUTTON_11    (GUI_ID_USER + 0x0F)
#define ID_BUTTON_12    (GUI_ID_USER + 0x10)
#define ID_BUTTON_13    (GUI_ID_USER + 0x11)
#define ID_BUTTON_14    (GUI_ID_USER + 0x12)
#define ID_BUTTON_15    (GUI_ID_USER + 0x13)
#define ID_BUTTON_16    (GUI_ID_USER + 0x14)
#define ID_BUTTON_17    (GUI_ID_USER + 0x15)
#define ID_BUTTON_18    (GUI_ID_USER + 0x16)
#define ID_BUTTON_19    (GUI_ID_USER + 0x17)
#define ID_BUTTON_20    (GUI_ID_USER + 0x18)
#define ID_BUTTON_21    (GUI_ID_USER + 0x19)
#define ID_BUTTON_22    (GUI_ID_USER + 0x1A)
#define ID_BUTTON_23    (GUI_ID_USER + 0x1B)
#define ID_BUTTON_24    (GUI_ID_USER + 0x1C)
#define ID_BUTTON_25    (GUI_ID_USER + 0x1D)
#define ID_BUTTON_26    (GUI_ID_USER + 0x1E)
#define ID_BUTTON_27    (GUI_ID_USER + 0x1F)
#define ID_BUTTON_28    (GUI_ID_USER + 0x20)
#define ID_BUTTON_29    (GUI_ID_USER + 0x21)
#define ID_BUTTON_30    (GUI_ID_USER + 0x22)
#define ID_BUTTON_31    (GUI_ID_USER + 0x23)
#define ID_BUTTON_32    (GUI_ID_USER + 0x24)
#define ID_BUTTON_33    (GUI_ID_USER + 0x25)
#define ID_BUTTON_34    (GUI_ID_USER + 0x26)
#define ID_BUTTON_35    (GUI_ID_USER + 0x27)
#define ID_BUTTON_36    (GUI_ID_USER + 0x28)
#define ID_BUTTON_37    (GUI_ID_USER + 0x29)
#define ID_BUTTON_38    (GUI_ID_USER + 0x2A)
#define ID_BUTTON_39    (GUI_ID_USER + 0x2B)
#define ID_BUTTON_40    (GUI_ID_USER + 0x2C)
#define ID_BUTTON_41    (GUI_ID_USER + 0x2D)
#define ID_BUTTON_42    (GUI_ID_USER + 0x2E)
#define ID_BUTTON_43    (GUI_ID_USER + 0x2F)
#define ID_BUTTON_44    (GUI_ID_USER + 0x30)
#define ID_BUTTON_45    (GUI_ID_USER + 0x31)
#define ID_BUTTON_46    (GUI_ID_USER + 0x32)
#define ID_BUTTON_47    (GUI_ID_USER + 0x33)
#define ID_BUTTON_48    (GUI_ID_USER + 0x34)
#define ID_BUTTON_49    (GUI_ID_USER + 0x35)
#define ID_BUTTON_50    (GUI_ID_USER + 0x36)
#define ID_BUTTON_51    (GUI_ID_USER + 0x37)
#define ID_BUTTON_52    (GUI_ID_USER + 0x38)
#define ID_BUTTON_53    (GUI_ID_USER + 0x39)
#define ID_BUTTON_54    (GUI_ID_USER + 0x3A)
#define ID_BUTTON_55    (GUI_ID_USER + 0x3B)
#define ID_BUTTON_56    (GUI_ID_USER + 0x3C)
#define ID_BUTTON_57    (GUI_ID_USER + 0x3D)
#define ID_BUTTON_58    (GUI_ID_USER + 0x3E)
#define ID_BUTTON_59    (GUI_ID_USER + 0x3F)
#define ID_BUTTON_60    (GUI_ID_USER + 0x40)
#define ID_BUTTON_61    (GUI_ID_USER + 0x41)
#define ID_BUTTON_62    (GUI_ID_USER + 0x42)
#define ID_BUTTON_63    (GUI_ID_USER + 0x43)
#define ID_BUTTON_64    (GUI_ID_USER + 0x44)
#define ID_BUTTON_65    (GUI_ID_USER + 0x45)
#define ID_BUTTON_66    (GUI_ID_USER + 0x46)
#define ID_BUTTON_67    (GUI_ID_USER + 0x47)
#define ID_BUTTON_68    (GUI_ID_USER + 0x48)
#define ID_BUTTON_69    (GUI_ID_USER + 0x49)
#define ID_BUTTON_70    (GUI_ID_USER + 0x4A)
#define ID_BUTTON_71    (GUI_ID_USER + 0x4B)
#define ID_BUTTON_72    (GUI_ID_USER + 0x4C)
#define ID_BUTTON_73    (GUI_ID_USER + 0x4D)
#define ID_BUTTON_74    (GUI_ID_USER + 0x4E)
#define ID_BUTTON_75    (GUI_ID_USER + 0x4F)
#define ID_BUTTON_76    (GUI_ID_USER + 0x50)
#define ID_BUTTON_77    (GUI_ID_USER + 0x51)
#define ID_BUTTON_78    (GUI_ID_USER + 0x52)
#define ID_BUTTON_79    (GUI_ID_USER + 0x53)
#define ID_BUTTON_80    (GUI_ID_USER + 0x54)
#define ID_BUTTON_81    (GUI_ID_USER + 0x55)
#define ID_BUTTON_82    (GUI_ID_USER + 0x56)
#define ID_BUTTON_83    (GUI_ID_USER + 0x57)
#define ID_BUTTON_84    (GUI_ID_USER + 0x58)
#define ID_BUTTON_85    (GUI_ID_USER + 0x59)
#define ID_BUTTON_86    (GUI_ID_USER + 0x5A)
#define ID_BUTTON_87    (GUI_ID_USER + 0x5B)
#define ID_BUTTON_88    (GUI_ID_USER + 0x5C)
#define ID_BUTTON_89    (GUI_ID_USER + 0x5D)
#define ID_BUTTON_90    (GUI_ID_USER + 0x5E)
#define ID_BUTTON_91    (GUI_ID_USER + 0x5F)
#define ID_BUTTON_92    (GUI_ID_USER + 0x60)
#define ID_BUTTON_93    (GUI_ID_USER + 0x61)
#define ID_BUTTON_94    (GUI_ID_USER + 0x62)
#define ID_BUTTON_95    (GUI_ID_USER + 0x63)
#define ID_BUTTON_96    (GUI_ID_USER + 0x64)
#define ID_BUTTON_97    (GUI_ID_USER + 0x65)
#define ID_BUTTON_98    (GUI_ID_USER + 0x66)
#define ID_BUTTON_99    (GUI_ID_USER + 0x67)
#define ID_BUTTON_100    (GUI_ID_USER + 0x68)
#define ID_BUTTON_101    (GUI_ID_USER + 0x69)
#define ID_BUTTON_102    (GUI_ID_USER + 0x6A)
#define ID_BUTTON_103    (GUI_ID_USER + 0x6B)
#define ID_BUTTON_104    (GUI_ID_USER + 0x6C)
#define ID_BUTTON_105    (GUI_ID_USER + 0x6D)
#define ID_BUTTON_106    (GUI_ID_USER + 0x6E)
#define ID_BUTTON_107    (GUI_ID_USER + 0x6F)
#define ID_BUTTON_108    (GUI_ID_USER + 0x70)
#define ID_BUTTON_109    (GUI_ID_USER + 0x71)
#define ID_BUTTON_110    (GUI_ID_USER + 0x72)
#define ID_BUTTON_111    (GUI_ID_USER + 0x73)
#define ID_BUTTON_112    (GUI_ID_USER + 0x74)
#define ID_BUTTON_113    (GUI_ID_USER + 0x75)
#define ID_BUTTON_114    (GUI_ID_USER + 0x76)
#define ID_BUTTON_115    (GUI_ID_USER + 0x77)
#define ID_BUTTON_116    (GUI_ID_USER + 0x78)
#define ID_BUTTON_117    (GUI_ID_USER + 0x79)
#define ID_BUTTON_118    (GUI_ID_USER + 0x7A)
#define ID_BUTTON_119    (GUI_ID_USER + 0x7B)



// USER START (Optionally insert additional defines)
extern WM_HWIN window,bomb,test_E;

extern int digital_num;// digital_num == 1 means button pressed one time, and so on. When press "C", digital_num = 0; when press "E",digital_num = 0;
extern int bomb_num ;
extern char buff[3];
extern int i;


extern int square_number[120];
extern char test[6];
extern int random_array[100];
extern int nearby_bomb;
extern volatile int queue[120];
extern int position_open_or_not[120];
extern int queue_add_index;
int testdigit = 0;
extern int playing_click;
extern int queue_index;
extern int queue_length;
extern int WinOrLoss;
extern GUI_TIMER_TIME OS_TimeMS;
int open_yet_floor = 0;
int time = 0;
int second = 0;
int minute = 0;
void visit_go(void);
// USER END

/*********************************************************************
*
*       Static data
*
**********************************************************************
*/

// USER START (Optionally insert additional static data)
// USER END

/*********************************************************************
*
*       _aDialogCreate
*/
static const GUI_WIDGET_CREATE_INFO _aDialogCreate[] = {
  { WINDOW_CreateIndirect, "BOMB", ID_WINDOW_0, 0, 0, 480, 272, WM_CF_MEMDEV_ON_REDRAW, 0x0, 0 },
  { EDIT_CreateIndirect, "Edit", ID_EDIT_0, 40, 20, 80, 20, 0, 0x64, 0 },
  { EDIT_CreateIndirect, "Edit", ID_EDIT_1, 360, 20, 80, 20, 0, 0x64, 0 },
  { TEXT_CreateIndirect, "Text", ID_TEXT_0, 210, 20, 80, 20, 0, 0x64, 0 },
  { BUTTON_CreateIndirect, "1b1", ID_BUTTON_0, 65, 50, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "1b2", ID_BUTTON_1, 90, 50, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "1b3", ID_BUTTON_2, 115, 50, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "1b4", ID_BUTTON_3, 140, 50, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "1b5", ID_BUTTON_4, 165, 50, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "1b6", ID_BUTTON_5, 190, 50, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "1b7", ID_BUTTON_6, 215, 50, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "1b8", ID_BUTTON_7, 240, 50, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "1b9", ID_BUTTON_8, 265, 50, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "1b10", ID_BUTTON_9, 290, 50, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "1b11", ID_BUTTON_10, 315, 50, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "1b12", ID_BUTTON_11, 340, 50, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "1b13", ID_BUTTON_12, 365, 50, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "1b14", ID_BUTTON_13, 390, 50, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "1b15", ID_BUTTON_14, 415, 50, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "2b1", ID_BUTTON_15, 65, 75, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "2b2", ID_BUTTON_16, 90, 75, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "2b3", ID_BUTTON_17, 115, 75, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "2b4", ID_BUTTON_18, 140, 75, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "2b5", ID_BUTTON_19, 165, 75, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "2b6", ID_BUTTON_20, 190, 75, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "2b7", ID_BUTTON_21, 215, 75, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "2b8", ID_BUTTON_22, 240, 75, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "2b9", ID_BUTTON_23, 265, 75, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "2b10", ID_BUTTON_24, 290, 75, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "2b11", ID_BUTTON_25, 315, 75, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "2b12", ID_BUTTON_26, 340, 75, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "2b13", ID_BUTTON_27, 365, 75, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "2b14", ID_BUTTON_28, 390, 75, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "2b15", ID_BUTTON_29, 415, 75, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "3b1", ID_BUTTON_30, 65, 100, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "3b2", ID_BUTTON_31, 90, 100, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "3b3", ID_BUTTON_32, 115, 100, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "3b4", ID_BUTTON_33, 140, 100, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "3b5", ID_BUTTON_34, 165, 100, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "3b6", ID_BUTTON_35, 190, 100, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "3b7", ID_BUTTON_36, 215, 100, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "3b8", ID_BUTTON_37, 240, 100, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "3b9", ID_BUTTON_38, 265, 100, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "3b10", ID_BUTTON_39, 290, 100, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "3b11", ID_BUTTON_40, 315, 100, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "3b12", ID_BUTTON_41, 340, 100, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "3b13", ID_BUTTON_42, 365, 100, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "3b14", ID_BUTTON_43, 390, 100, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "3b15", ID_BUTTON_44, 415, 100, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "4b1", ID_BUTTON_45, 65, 125, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "4b2", ID_BUTTON_46, 90, 125, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "4b3", ID_BUTTON_47, 115, 125, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "4b4", ID_BUTTON_48, 140, 125, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "4b5", ID_BUTTON_49, 165, 125, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "4b6", ID_BUTTON_50, 190, 125, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "4b7", ID_BUTTON_51, 215, 125, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "4b8", ID_BUTTON_52, 240, 125, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "4b9", ID_BUTTON_53, 265, 125, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "4b10", ID_BUTTON_54, 290, 125, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "4b11", ID_BUTTON_55, 315, 125, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "4b12", ID_BUTTON_56, 340, 125, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "4b13", ID_BUTTON_57, 365, 125, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "4b14", ID_BUTTON_58, 390, 125, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "4b15", ID_BUTTON_59, 415, 125, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "5b1", ID_BUTTON_60, 65, 150, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "5b2", ID_BUTTON_61, 90, 150, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "5b3", ID_BUTTON_62, 115, 150, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "5b4", ID_BUTTON_63, 140, 150, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "5b5", ID_BUTTON_64, 165, 150, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "5b6", ID_BUTTON_65, 190, 150, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "5b7", ID_BUTTON_66, 215, 150, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "5b8", ID_BUTTON_67, 240, 150, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "5b9", ID_BUTTON_68, 265, 150, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "5b10", ID_BUTTON_69, 290, 150, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "5b11", ID_BUTTON_70, 315, 150, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "5b12", ID_BUTTON_71, 340, 150, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "5b13", ID_BUTTON_72, 365, 150, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "5b14", ID_BUTTON_73, 390, 150, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "5b15", ID_BUTTON_74, 415, 150, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "6b1", ID_BUTTON_75, 65, 175, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "6b2", ID_BUTTON_76, 90, 175, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "6b3", ID_BUTTON_77, 115, 175, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "6b4", ID_BUTTON_78, 140, 175, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "6b5", ID_BUTTON_79, 165, 175, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "6b6", ID_BUTTON_80, 190, 175, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "6b7", ID_BUTTON_81, 215, 175, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "6b8", ID_BUTTON_82, 240, 175, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "6b9", ID_BUTTON_83, 265, 175, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "6b10", ID_BUTTON_84, 290, 175, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "6b11", ID_BUTTON_85, 315, 175, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "6b12", ID_BUTTON_86, 340, 175, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "6b13", ID_BUTTON_87, 365, 175, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "6b14", ID_BUTTON_88, 390, 175, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "6b15", ID_BUTTON_89, 415, 175, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "7b1", ID_BUTTON_90, 65, 200, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "7b2", ID_BUTTON_91, 90, 200, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "7b3", ID_BUTTON_92, 115, 200, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "7b4", ID_BUTTON_93, 140, 200, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "7b5", ID_BUTTON_94, 165, 200, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "7b6", ID_BUTTON_95, 190, 200, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "7b7", ID_BUTTON_96, 215, 200, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "7b8", ID_BUTTON_97, 240, 200, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "7b9", ID_BUTTON_98, 265, 200, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "7b10", ID_BUTTON_99, 290, 200, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "7b11", ID_BUTTON_100, 315, 200, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "7b12", ID_BUTTON_101, 340, 200, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "7b13", ID_BUTTON_102, 365, 200, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "7b14", ID_BUTTON_103, 390, 200, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "7b15", ID_BUTTON_104, 415, 200, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "8b1", ID_BUTTON_105, 65, 225, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "8b2", ID_BUTTON_106, 90, 225, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "8b3", ID_BUTTON_107, 115, 225, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "8b4", ID_BUTTON_108, 140, 225, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "8b5", ID_BUTTON_109, 165, 225, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "8b6", ID_BUTTON_110, 190, 225, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "8b7", ID_BUTTON_111, 215, 225, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "8b8", ID_BUTTON_112, 240, 225, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "8b9", ID_BUTTON_113, 265, 225, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "8b10", ID_BUTTON_114, 290, 225, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "8b11", ID_BUTTON_115, 315, 225, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "8b12", ID_BUTTON_116, 340, 225, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "8b13", ID_BUTTON_117, 365, 225, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "8b14", ID_BUTTON_118, 390, 225, 25, 25, 0, 0x0, 0 },
  { BUTTON_CreateIndirect, "8b15", ID_BUTTON_119, 415, 225, 25, 25, 0, 0x0, 0 },
  // USER START (Optionally insert additional widgets)
  // USER END
};

/*********************************************************************
*
*       Static code
*
**********************************************************************
*/

// USER START (Optionally insert additional static code)
extern GUI_CONST_STORAGE GUI_BITMAP bmfloor;
extern GUI_CONST_STORAGE GUI_BITMAP bm01;
extern GUI_CONST_STORAGE GUI_BITMAP bm02;
extern GUI_CONST_STORAGE GUI_BITMAP bm03;
extern GUI_CONST_STORAGE GUI_BITMAP bm04;
extern GUI_CONST_STORAGE GUI_BITMAP bm05;
extern GUI_CONST_STORAGE GUI_BITMAP bm06;
extern GUI_CONST_STORAGE GUI_BITMAP bm07;
extern GUI_CONST_STORAGE GUI_BITMAP bm08;
extern GUI_CONST_STORAGE GUI_BITMAP bmsave;
extern GUI_CONST_STORAGE GUI_BITMAP bmbomb_image;
GUI_BITMAP buttonbmp_tab[11];
// USER END

/*********************************************************************
*
*       _cbDialog
*/
static void _cbDialog(WM_MESSAGE * pMsg) {
  WM_HWIN hItem;
  WM_HWIN hWin;
  int     NCode;
  int     Id;
  // USER START (Optionally insert additional variables)
  // USER END
  hWin = pMsg->hWin;
  switch (pMsg->MsgId) {
  case WM_INIT_DIALOG:
    //
    // Initialization of 'Edit'
    //
    hItem = WM_GetDialogItem(pMsg->hWin, ID_EDIT_0);
    EDIT_SetFont(hItem, GUI_FONT_20_1);
    EDIT_SetText(hItem, buff);
    //
    // Initialization of 'Edit'
    //
    hItem = WM_GetDialogItem(pMsg->hWin, ID_EDIT_1);
    EDIT_SetText(hItem, "00:00");
    EDIT_SetFont(hItem, GUI_FONT_20_1);
    //
    // Initialization of 'Text'
    //
    hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
    TEXT_SetText(hItem, "");
    TEXT_SetFont(hItem, GUI_FONT_20_1);
    // USER START (Optionally insert additional code for further widget initialization)
    WM_CreateTimer(hWin, 0, 100, 0);

		for(;i < 120 ; i++){
			hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + i);
			BUTTON_SetText(hItem," ");
			BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[0],0,0);
		}
    // USER END
    break;
  case WM_TIMER:
    WM_RestartTimer(pMsg->Data.v, 100);
    time++;
    if(time % 10 == 0){
      second++;
      if(second%60 == 0){
        second = 0;
        minute++;
      }
      if(second < 10 && minute < 10){
        sprintf(test,"0%d:0%d",minute,second);
      }
      else if(second >= 10 && minute < 10){
        sprintf(test,"0%d:%d",minute,second);
      }
      else if(second < 10 && minute >= 10){
        sprintf(test,"%d:0%d",minute,second);
      }
      hItem = WM_GetDialogItem(pMsg->hWin, ID_EDIT_1);
      EDIT_SetText(hItem, test);
    }
    break;
  case WM_NOTIFY_PARENT:
    Id    = WM_GetId(pMsg->hWinSrc);
    NCode = pMsg->Data.v;
    switch(Id) {
    case ID_EDIT_0: // Notifications sent by 'Edit'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      case WM_NOTIFICATION_VALUE_CHANGED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_EDIT_1: // Notifications sent by 'Edit'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      case WM_NOTIFICATION_VALUE_CHANGED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_0: // Notifications sent by '1b1'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 0;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        //saving code
        //for(int z = 0 ; z < bomb_num ; z++){
          //hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[z]);
          //BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[10],0,0);
        //}
        //..................................//

        //saving code part 2
        /*for(int z = 0 ; z < 120 ; z++){
          hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + z);
          BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[z]],0,0);
        }*/
        
				//hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0);
				//BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[10],0,0);
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_1: // Notifications sent by '1b2'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 1;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_2: // Notifications sent by '1b3'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 2;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_3: // Notifications sent by '1b4'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 3;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_4: // Notifications sent by '1b5'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 4;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_5: // Notifications sent by '1b6'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 5;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_6: // Notifications sent by '1b7'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 6;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_7: // Notifications sent by '1b8'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 7;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_8: // Notifications sent by '1b9'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 8;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_9: // Notifications sent by '1b10'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 9;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_10: // Notifications sent by '1b11'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 10;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_11: // Notifications sent by '1b12'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 11;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_12: // Notifications sent by '1b13'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 12;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_13: // Notifications sent by '1b14'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 13;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_14: // Notifications sent by '1b15'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 14;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_15: // Notifications sent by '2b1'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 15;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_16: // Notifications sent by '2b2'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 16;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_17: // Notifications sent by '2b3'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 17;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_18: // Notifications sent by '2b4'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 18;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_19: // Notifications sent by '2b5'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 19;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_20: // Notifications sent by '2b6'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 20;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_21: // Notifications sent by '2b7'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 21;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_22: // Notifications sent by '2b8'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 22;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_23: // Notifications sent by '2b9'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 23;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_24: // Notifications sent by '2b10'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 24;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_25: // Notifications sent by '2b11'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 25;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_26: // Notifications sent by '2b12'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 26;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_27: // Notifications sent by '2b13'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 27;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_28: // Notifications sent by '2b14'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 28;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_29: // Notifications sent by '2b15'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 29;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_30: // Notifications sent by '3b1'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 30;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_31: // Notifications sent by '3b2'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 31;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_32: // Notifications sent by '3b3'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 32;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_33: // Notifications sent by '3b4'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 33;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_34: // Notifications sent by '3b5'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 34;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_35: // Notifications sent by '3b6'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 35;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_36: // Notifications sent by '3b7'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 36;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_37: // Notifications sent by '3b8'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 37;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_38: // Notifications sent by '3b9'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 38;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_39: // Notifications sent by '3b10'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 39;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_40: // Notifications sent by '3b11'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 40;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_41: // Notifications sent by '3b12'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 41;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_42: // Notifications sent by '3b13'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 42;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_43: // Notifications sent by '3b14'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 43;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_44: // Notifications sent by '3b15'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 44;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_45: // Notifications sent by '4b1'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 45;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_46: // Notifications sent by '4b2'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 46;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_47: // Notifications sent by '4b3'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 47;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_48: // Notifications sent by '4b4'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 48;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_49: // Notifications sent by '4b5'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 49;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_50: // Notifications sent by '4b6'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 50;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_51: // Notifications sent by '4b7'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 51;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_52: // Notifications sent by '4b8'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 52;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_53: // Notifications sent by '4b9'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 53;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_54: // Notifications sent by '4b10'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 54;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_55: // Notifications sent by '4b11'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 55;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_56: // Notifications sent by '4b12'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 56;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_57: // Notifications sent by '4b13'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 57;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_58: // Notifications sent by '4b14'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 58;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_59: // Notifications sent by '4b15'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 59;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_60: // Notifications sent by '5b1'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 60;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_61: // Notifications sent by '5b2'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 61;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_62: // Notifications sent by '5b3'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 62;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_63: // Notifications sent by '5b4'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 63;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_64: // Notifications sent by '5b5'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 64;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_65: // Notifications sent by '5b6'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 65;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_66: // Notifications sent by '5b7'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 66;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_67: // Notifications sent by '5b8'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 67;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_68: // Notifications sent by '5b9'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 68;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_69: // Notifications sent by '5b10'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 69;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_70: // Notifications sent by '5b11'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 70;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_71: // Notifications sent by '5b12'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 71;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_72: // Notifications sent by '5b13'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 72;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_73: // Notifications sent by '5b14'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 73;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_74: // Notifications sent by '5b15'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 74;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_75: // Notifications sent by '6b1'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 75;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_76: // Notifications sent by '6b2'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 76;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_77: // Notifications sent by '6b3'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 77;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_78: // Notifications sent by '6b4'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 78;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_79: // Notifications sent by '6b5'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 79;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_80: // Notifications sent by '6b6'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 80;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_81: // Notifications sent by '6b7'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 81;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_82: // Notifications sent by '6b8'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 82;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_83: // Notifications sent by '6b9'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 83;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_84: // Notifications sent by '6b10'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 84;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_85: // Notifications sent by '6b11'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 85;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_86: // Notifications sent by '6b12'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 86;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_87: // Notifications sent by '6b13'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 87;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_88: // Notifications sent by '6b14'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 88;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_89: // Notifications sent by '6b15'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 89;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_90: // Notifications sent by '7b1'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 90;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_91: // Notifications sent by '7b2'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 91;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_92: // Notifications sent by '7b3'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 92;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_93: // Notifications sent by '7b4'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 93;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_94: // Notifications sent by '7b5'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 94;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_95: // Notifications sent by '7b6'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 95;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_96: // Notifications sent by '7b7'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 96;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_97: // Notifications sent by '7b8'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 97;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_98: // Notifications sent by '7b9'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 98;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_99: // Notifications sent by '7b10'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 99;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_100: // Notifications sent by '7b11'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 100;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_101: // Notifications sent by '7b12'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 101;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_102: // Notifications sent by '7b13'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 102;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_103: // Notifications sent by '7b14'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 103;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_104: // Notifications sent by '7b15'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 104;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_105: // Notifications sent by '8b1'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 105;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_106: // Notifications sent by '8b2'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 106;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_107: // Notifications sent by '8b3'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 107;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_108: // Notifications sent by '8b4'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 108;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_109: // Notifications sent by '8b5'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 109;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_110: // Notifications sent by '8b6'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 110;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_111: // Notifications sent by '8b7'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 111;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_112: // Notifications sent by '8b8'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 112;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_113: // Notifications sent by '8b9'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 113;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_114: // Notifications sent by '8b10'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 114;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_115: // Notifications sent by '8b11'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 115;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_116: // Notifications sent by '8b12'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 116;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_117: // Notifications sent by '8b13'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 117;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_118: // Notifications sent by '8b14'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 118;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    case ID_BUTTON_119: // Notifications sent by '8b15'
      switch(NCode) {
      case WM_NOTIFICATION_CLICKED:
        // USER START (Optionally insert code for reacting on notification message)
        queue_index = 0;
        queue[queue_index] = 119;
        open_yet_floor = 0;
        if(square_number[queue[queue_index]] == 10){
          WinOrLoss = -1;
					hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					TEXT_SetText(hItem,"BOMB");
          for(int k = 0 ; k < bomb_num ; k++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + random_array[k]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[random_array[k]]],0,0);
          }
        }
        else{
          position_open_or_not[queue[queue_index]] = 1;
          visit_go();
          for(int z = 0 ; z < 120 ; z ++){
            if(position_open_or_not[z] == 0){
              open_yet_floor++;
            }
          }
          if(open_yet_floor == 0){
            WinOrLoss = 1;
            hItem = WM_GetDialogItem(pMsg->hWin, ID_TEXT_0);
					  TEXT_SetText(hItem,"Win");
          }
          for(int z = 0 ; z < queue_length ; z ++){
            hItem = WM_GetDialogItem(pMsg->hWin, ID_BUTTON_0 + queue[z]);
            BUTTON_SetBitmapEx(hItem,0,&buttonbmp_tab[square_number[queue[z]]],0,0);
            queue[z] = -1;
          }
          queue_length = 0;
        }
        // USER END
        break;
      case WM_NOTIFICATION_RELEASED:
        // USER START (Optionally insert code for reacting on notification message)
        // USER END
        break;
      // USER START (Optionally insert additional code for further notification handling)
      // USER END
      }
      break;
    // USER START (Optionally insert additional code for further Ids)
    // USER END
    }
    break;
  // USER START (Optionally insert additional message handling)
  // USER END
  default:
    WM_DefaultProc(pMsg);
    break;
  }
}

/*********************************************************************
*
*       Public code
*
**********************************************************************
*/
/*********************************************************************
*
*       CreateBOMB
*/
WM_HWIN CreateBOMB(void);
WM_HWIN CreateBOMB(void) {
  WM_HWIN hWin;
	buttonbmp_tab[0]= bmfloor;
	buttonbmp_tab[1] = bm01;
	buttonbmp_tab[2] = bm02;
	buttonbmp_tab[3] = bm03;
	buttonbmp_tab[4] = bm04;
	buttonbmp_tab[5] = bm05;
	buttonbmp_tab[6] = bm06;
	buttonbmp_tab[7] = bm07;
	buttonbmp_tab[8] = bm08;
	buttonbmp_tab[9] = bmsave;
	buttonbmp_tab[10] = bmbomb_image;
  hWin = GUI_CreateDialogBox(_aDialogCreate, GUI_COUNTOF(_aDialogCreate), _cbDialog, WM_HBKWIN, 0, 0);
  return hWin;
}

// USER START (Optionally insert additional public code)
void visit_go(void){
    for(queue_index = 0, queue_add_index = 1; queue[queue_index] > -1 ; queue_index ++){
            if(square_number[queue[queue_index]] == 9){
                switch(queue[queue_index]){
                    case 0: // ok
											
                        if(square_number[queue[queue_index] + 1] != 10 && position_open_or_not[queue[queue_index] + 1] == 0){
                            queue[queue_add_index] = queue[queue_index] + 1;
                            queue_add_index++;
                            position_open_or_not[queue[queue_index] + 1] = 1;
                        }
                        if(square_number[queue[queue_index] + 15] != 10 && position_open_or_not[queue[queue_index] + 15] == 0){
                            queue[queue_add_index] = queue[queue_index] + 15;
                            queue_add_index++;
                            position_open_or_not[queue[queue_index] + 15] = 1;
                        }
                        if(square_number[queue[queue_index] + 16] != 10 && position_open_or_not[queue[queue_index] + 16] == 0){
                            queue[queue_add_index] = queue[queue_index] + 16;
                            queue_add_index++;
                            position_open_or_not[queue[queue_index] + 16] = 1;
                        }
                    break;
                    case 14: //ok
                        if(square_number[queue[queue_index] - 1] != 10 && position_open_or_not[queue[queue_index] - 1] == 0){
                            queue[queue_add_index] = queue[queue_index] - 1;
                            queue_add_index++;
                            position_open_or_not[queue[queue_index] - 1] = 1;
                        }
                        if(square_number[queue[queue_index] + 14] != 10 && position_open_or_not[queue[queue_index] + 14] == 0){
                            queue[queue_add_index] = queue[queue_index] + 14;
                            queue_add_index++;
                            position_open_or_not[queue[queue_index] + 14] = 1;
                        }
                        if(square_number[queue[queue_index] + 15] != 10 && position_open_or_not[queue[queue_index] + 15] == 0){
                            queue[queue_add_index] = queue[queue_index] + 15;
                            queue_add_index++;
                            position_open_or_not[queue[queue_index] + 15] = 1;
                        }
                    break;
                    case 105: //ok
                        if(square_number[queue[queue_index] - 15] != 10 && position_open_or_not[queue[queue_index] - 15] == 0){
                            queue[queue_add_index] = queue[queue_index] - 15;
                            queue_add_index++;
                            position_open_or_not[queue[queue_index] - 15] = 1;
                        }
                        if(square_number[queue[queue_index] - 14] != 10 && position_open_or_not[queue[queue_index] - 14] == 0){
                            queue[queue_add_index] = queue[queue_index] - 14;
                            queue_add_index++;
                            position_open_or_not[queue[queue_index] - 14] = 1;
                        }
                        if(square_number[queue[queue_index] + 1] != 10 && position_open_or_not[queue[queue_index] + 1] == 0){
                            queue[queue_add_index] = queue[queue_index] + 1;
                            queue_add_index++;
                            position_open_or_not[queue[queue_index] + 1] = 1;
                        }
                    break;
                    case 119: //ok
                        if(square_number[queue[queue_index] - 16] != 10 && position_open_or_not[queue[queue_index] - 16] == 0){
                            queue[queue_add_index] = queue[queue_index] - 16;
                            queue_add_index++;
                            position_open_or_not[queue[queue_index] - 16] = 1;
                        }
                        if(square_number[queue[queue_index] - 15] != 10 && position_open_or_not[queue[queue_index] - 15] == 0){
                            queue[queue_add_index] = queue[queue_index] - 15;
                            queue_add_index++;
                            position_open_or_not[queue[queue_index] - 15] = 1;
                        }
                        if(square_number[queue[queue_index] - 1] != 10 && position_open_or_not[queue[queue_index] - 1] == 0){
                            queue[queue_add_index] = queue[queue_index] - 1;
                            queue_add_index++;
                            position_open_or_not[queue[queue_index] - 1] = 1;
                        }
                    break;
                    case 1: case 2: case 3: case 4: case 5: case 6: case 7: case 8: case 9: case 10: case 11: case 12: case 13: //ok
                        if(square_number[queue[queue_index] - 1] != 10 && position_open_or_not[queue[queue_index] - 1] == 0){
                            queue[queue_add_index] = queue[queue_index] - 1;
                            queue_add_index++;
                            position_open_or_not[queue[queue_index] - 1] = 1;
                        }
                        if(square_number[queue[queue_index] + 1] != 10 && position_open_or_not[queue[queue_index] + 1] == 0){
                            queue[queue_add_index] = queue[queue_index] + 1;
                            queue_add_index++;
                            position_open_or_not[queue[queue_index] + 1] = 1;
                        }
                        if(square_number[queue[queue_index] + 14] != 10 && position_open_or_not[queue[queue_index] + 14] == 0){
                            queue[queue_add_index] = queue[queue_index] + 14;
                            queue_add_index++;
                            position_open_or_not[queue[queue_index] + 14] = 1;
                        }
                        if(square_number[queue[queue_index] + 15] != 10 && position_open_or_not[queue[queue_index] + 15] == 0){
                            queue[queue_add_index] = queue[queue_index] + 15;
                            queue_add_index++;
                            position_open_or_not[queue[queue_index] + 15] = 1;
                        }
                        if(square_number[queue[queue_index] + 16] != 10 && position_open_or_not[queue[queue_index] + 16] == 0){
                            queue[queue_add_index] = queue[queue_index] + 16;
                            queue_add_index++;
                            position_open_or_not[queue[queue_index] + 16] = 1;
                        }
                    break;
                    case 106: case 107: case 108: case 109: case 110: case 111: case 112: case 113: case 114: case 115: case 116: case 117: case 118:
                        if(square_number[queue[queue_index] - 16] != 10 && position_open_or_not[queue[queue_index] - 16] == 0){
                            queue[queue_add_index] = queue[queue_index] - 16;
                            queue_add_index++;
                            position_open_or_not[queue[queue_index] - 16] = 1;
                        }
                        if(square_number[queue[queue_index] - 15] != 10 && position_open_or_not[queue[queue_index] - 15] == 0){
                            queue[queue_add_index] = queue[queue_index] - 15;
                            queue_add_index++;
                            position_open_or_not[queue[queue_index] - 15] = 1;
                        }
                        if(square_number[queue[queue_index] - 14] != 10 && position_open_or_not[queue[queue_index] - 14] == 0){
                            queue[queue_add_index] = queue[queue_index] - 14;
                            queue_add_index++;
                            position_open_or_not[queue[queue_index] - 14] = 1;
                        }
                        if(square_number[queue[queue_index] - 1] != 10 && position_open_or_not[queue[queue_index] - 1] == 0){
                            queue[queue_add_index] = queue[queue_index] - 1;
                            queue_add_index++;
                            position_open_or_not[queue[queue_index] - 1] = 1;
                        }
                        if(square_number[queue[queue_index] + 1] != 10 && position_open_or_not[queue[queue_index] + 1] == 0){
                            queue[queue_add_index] = queue[queue_index] + 1;
                            queue_add_index++;
                            position_open_or_not[queue[queue_index] + 1] = 1;
                        }
                    break;
                    case 15: case 30: case 45: case 60: case 75: case 90: //ok
                        if(square_number[queue[queue_index] - 15] != 10 && position_open_or_not[queue[queue_index] - 15] == 0){
                            queue[queue_add_index] = queue[queue_index] - 15;
                            queue_add_index++;
                            position_open_or_not[queue[queue_index] - 15] = 1;
                        }
                        if(square_number[queue[queue_index] - 14] != 10 && position_open_or_not[queue[queue_index] - 14] == 0){
                            queue[queue_add_index] = queue[queue_index] - 14;
                            queue_add_index++;
                            position_open_or_not[queue[queue_index] - 14] = 1;
                        }
                        if(square_number[queue[queue_index] + 1] != 10 && position_open_or_not[queue[queue_index] + 1] == 0){
                            queue[queue_add_index] = queue[queue_index] + 1;
                            queue_add_index++;
                            position_open_or_not[queue[queue_index] + 1] = 1;
                        }if(square_number[queue[queue_index] + 15] != 10 && position_open_or_not[queue[queue_index] + 15] == 0){
                            queue[queue_add_index] = queue[queue_index] + 15;
                            queue_add_index++;
                            position_open_or_not[queue[queue_index] + 15] = 1;
                        }
                        if(square_number[queue[queue_index] + 16] != 10 && position_open_or_not[queue[queue_index] + 16] == 0){
                            queue[queue_add_index] = queue[queue_index] + 16;
                            queue_add_index++;
                            position_open_or_not[queue[queue_index] + 16] = 1;
                        }
                    break;
                    case 29: case 44: case 59: case 74: case 89: case 104:
                        if(square_number[queue[queue_index] - 16] != 10 && position_open_or_not[queue[queue_index] - 16] == 0){
                            queue[queue_add_index] = queue[queue_index] - 16;
                            queue_add_index++;
                            position_open_or_not[queue[queue_index] - 16] = 1;
                        }
                        if(square_number[queue[queue_index] - 15] != 10 && position_open_or_not[queue[queue_index] - 15] == 0){
                            queue[queue_add_index] = queue[queue_index] - 15;
                            queue_add_index++;
                            position_open_or_not[queue[queue_index] - 15] = 1;
                        }
                        if(square_number[queue[queue_index] - 1] != 10 && position_open_or_not[queue[queue_index] - 1] == 0){
                            queue[queue_add_index] = queue[queue_index] - 1;
                            queue_add_index++;
                            position_open_or_not[queue[queue_index] - 1] = 1;
                        }if(square_number[queue[queue_index] + 14] != 10 && position_open_or_not[queue[queue_index] + 14] == 0){
                            queue[queue_add_index] = queue[queue_index] + 14;
                            queue_add_index++;
                            position_open_or_not[queue[queue_index] + 14] = 1;
                        }
                        if(square_number[queue[queue_index] + 15] != 10 && position_open_or_not[queue[queue_index] + 15] == 0){
                            queue[queue_add_index] = queue[queue_index] + 15;
                            queue_add_index++;
                            position_open_or_not[queue[queue_index] + 15] = 1;
                        }
                    break;
                    default:
                        if(square_number[queue[queue_index] - 16] != 10 && position_open_or_not[queue[queue_index] - 16] == 0){
                            queue[queue_add_index] = queue[queue_index] - 16;
                            queue_add_index++;
                            position_open_or_not[queue[queue_index] - 16] = 1;
                        }
                        if(square_number[queue[queue_index] - 15] != 10 && position_open_or_not[queue[queue_index] - 15] == 0){
                            queue[queue_add_index] = queue[queue_index] - 15;
                            queue_add_index++;
                            position_open_or_not[queue[queue_index] - 15] = 1;
                        }
                        if(square_number[queue[queue_index] - 14] != 10 && position_open_or_not[queue[queue_index] - 14] == 0){
                            queue[queue_add_index] = queue[queue_index] - 14;
                            queue_add_index++;
                            position_open_or_not[queue[queue_index] - 14] = 1;
                        }
                        if(square_number[queue[queue_index] - 1] != 10 && position_open_or_not[queue[queue_index] - 1] == 0){
                            queue[queue_add_index] = queue[queue_index] - 1;
                            queue_add_index++;
                            position_open_or_not[queue[queue_index] - 1] = 1;
                        }
                        if(square_number[queue[queue_index] + 1] != 10 && position_open_or_not[queue[queue_index] + 1] == 0){
                            queue[queue_add_index] = queue[queue_index] + 1;
                            queue_add_index++;
                            position_open_or_not[queue[queue_index] + 1] = 1;
                        }
                        if(square_number[queue[queue_index] + 14] != 10 && position_open_or_not[queue[queue_index] + 14] == 0){
                            queue[queue_add_index] = queue[queue_index] + 14;
                            queue_add_index++;
                            position_open_or_not[queue[queue_index] + 14] = 1;
                        }
                        if(square_number[queue[queue_index] + 15] != 10 && position_open_or_not[queue[queue_index] + 15] == 0){
                            queue[queue_add_index] = queue[queue_index] + 15;
                            queue_add_index++;
                            position_open_or_not[queue[queue_index] + 15] = 1;
                        }
                        if(square_number[queue[queue_index] + 16] != 10 && position_open_or_not[queue[queue_index] + 16] == 0){
                            queue[queue_add_index] = queue[queue_index] + 16;
                            queue_add_index++;
                            position_open_or_not[queue[queue_index] + 16] = 1;
                        }
                    break;
                }
            }
        }
        queue_length = queue_index;
}
// USER END

/*************************** End of file ****************************/
